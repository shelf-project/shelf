# v0.5 promotion — close the cluster-gated chapter

**Runs**: after [soak-tracker.md](soak-tracker.md) has 14 consecutive green days.
**Owner**: shelf-core.
**Status**: gated on soak completion.

## What this closes

Rollout-v1's entire point: after the 14-day soak is green on all four replicas,
we collapse the "cluster-gated" chapter that
[cluster-handoff.md](../cluster-handoff.md) defined. The following tickets
are marked **closed** in that doc and removed from the active backlog:

| ticket     | how rollout-v1 closed it                                                                                     |
| ---------- | ------------------------------------------------------------------------------------------------------------ |
| SHELF-13   | Shadow-mirror on rep-2 was substituted with hourly correctness-diff harness + per-replica canary; plan §3.    |
| SHELF-14   | E1/E3/E10/E12 signal is now captured continuously by the live dashboard + alerts + correctness-diff reports.  |
| SHELF-18   | Runtime NVMe PVC proof: four replicas ran on the StatefulSet throughout the 14-day soak.                      |
| SHELF-20   | Pod-rotation conformance measured on the weekly soak pod-roll (see [soak-tracker.md](soak-tracker.md)).      |
| SHELF-21   | 5-pod StatefulSet has been carrying 4-replica Trino traffic for 14 days; anti-affinity + NVMe confirmed.     |
| SHELF-27   | Live-alerting dashboard + rules with `replica` label carried the full rollout + soak (SHELF-27a landed).     |
| SHELF-28   | Kill-switch rehearsal + live kill-switch readiness throughout rollout; pages (= 0) confirms alert wiring.    |

## Promotion steps

### 1. Tag the release

```bash
cd /Users/aamir/trino/shelf
git checkout main && git pull
git tag -a v0.5 -m "Shelf v0.5 — 4-replica Trino Iceberg cache, 14-day soak green"
git push origin v0.5
```

### 2. Update cluster-handoff.md

A PR against [cluster-handoff.md](../cluster-handoff.md) landing the edits
captured in [cluster-handoff-v0.5-diff.md](cluster-handoff-v0.5-diff.md):
- Replace the cluster-gated tickets table with a "CLOSED by rollout-v1"
  footnote pointing to this doc.
- Retire the v0.5 gate section — it's now a past event. The content moves
  to the rollout-v1 index under "History".
- Remove the "Shelf pre-handoff delivery" mermaid timeline or rename it
  "Shelf pre-v0.5 delivery" and add a rollout-v1 section below it.
- Update "Pointers for the ops takeover" to reflect that ops *has* taken
  over; those pointers move into `oncall.md` as steady-state references.

### 3. Update the changelog

Append to [changelog.md](../changelog.md):

```markdown
## v0.5 — $(date -u +%Y-%m-%d)

Production rollout to all four penpencil Trino replicas for the Iceberg
catalog. 14-day soak on cumulative 4-replica traffic held the v0.5
criteria continuously. Rollout method: compressed canary per
`docs/rollout-v1.md`.

- SHELF-13/14/18/20/21/27/28: closed by rollout-v1.
- SHELF-27a: per-replica `replica` label shipped before rep-2 cutover.
- SHELF-26 replay tool gained a `prewarm` subcommand for per-replica
  cache seeding (benchmarks/trino_logs).
- Correctness-diff harness (benchmarks/correctness-diff) ran 14 × 24 = 336
  hourly comparisons during the soak with zero non-match rows.
- Capacity plan §4 ("v1 cluster target") promoted from extrapolation to
  measured; resize to 640 GiB NVMe deferred unless soak-tracker NVMe row
  ever tipped yellow.
```

### 4. Close the stale cluster-gated section

Add a small "History" subsection at the bottom of
[cluster-handoff.md](../cluster-handoff.md):

```markdown
## History

- 2026-04-XX: v0.5 promoted. Cluster-gated tickets (SHELF-13/14/18/20/21/27/28)
  closed by `rollout-v1`. See `docs/rollout-v1.md` for the rollout narrative
  and `docs/rollout-v1/v0.5-promote.md` for the closeout.
```

### 5. Archive the rollout artifacts

The completed result sheets —
`killswitch-rehearsal-results.md`, `cutover-rep{2,0,1,3}-results.md`,
and the filled-in `soak-tracker.md` table — stay on `main` under
`docs/rollout-v1/`. They're the audit trail; do not delete.

### 6. Follow-ups (not blocking v0.5)

- **Capacity re-size** — if the T+12h rep-3 cutover measurement (see
  [cutover-rep3.md](cutover-rep3.md)) flagged a tolerance miss, schedule
  a storage bump.
- **Prom/alert cleanup** — any alert that fired during rollout but was
  benign (e.g., `ShelfReadPathHitRatioCollapsed` during prewarm) gets a
  `for` duration bump or a condition refinement.
- **Foyer heuristics** — with 14 days of 4-replica production data, the
  admission heuristic in BLUEPRINT.md §9 can be tuned from E5 assumptions
  to measured.
- **SHELF-17a FrozenHot** — re-evaluate now that a real rep-2 trace is
  available.

## Rollback note

If somehow v0.5 is tagged in error (for example, the soak-tracker day-14
sign-off was incorrect and a red cell was missed), the v0.5 tag is
advisory; we do not re-tag or delete. Instead, land an errata entry in
[changelog.md](../changelog.md) and address via a minor release (`v0.5.1`)
with a clarifying note.

## Handoff complete

After step 4 lands, the cluster-handoff.md chapter is *done*. Ops has
fully adopted Shelf; this doc and the rollout-v1 tree are the historical
record. Future work runs as normal feature tickets, not rollout tickets.
