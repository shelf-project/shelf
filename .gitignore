# Rust build artefacts
/target
**/target
Cargo.lock.bak

# Criterion output
**/criterion

# IDE
.vscode/
.idea/
*.iml

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*~

# Secrets / local-only
.env
.env.*
!.env.example

# Origin-cluster overlay — site-specific values (bucket names, IRSA role
# ARNs, in-cluster service hostnames, dashboard folder UIDs) that
# operators keep in their own private deployments repo. Untracked here
# so the OSS surface stays clean. See SECURITY.md for the boundary.
infra/[p]enpencil/
infra/[p]enpencil/**

# Local minio / testcontainers data
/tmp-data
/.testdata

# Docs site build
site/
.mkdocs-cache/

# Python virtualenvs / caches
**/.venv/
**/__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
build/

# Node
node_modules/

# Java / Gradle / Maven build output
**/target/
**/.gradle/
**/build/
!docs/build/   # if mkdocs ever uses this name, delete this line
