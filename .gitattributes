# OSS-publish hygiene — strip the AI-iteration honest-history dirs and the
# rollout narrative from `git archive` tarballs (per launch playbook §brand).
# The origin-cluster overlay (`infra/[p]enpencil/`) is no longer tracked at
# all; see `.gitignore` for the boundary.
agents/out export-ignore
agents/out/** export-ignore
docs/rollout-v1 export-ignore
docs/rollout-v1/** export-ignore

# Common dev artefacts that don't belong in source tarballs
.github export-ignore
.github/** export-ignore
.cursor export-ignore
.cursor/** export-ignore

# Image assets are binary
*.png binary
*.jpg binary
*.jpeg binary
